{
  "name": "bweigel-lambda-layers",
  "version": "0.1.0",
  "bin": {
    "lambda-layers": "bin/lambda-layers.js"
  },
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "build:layers": "/bin/bash -c \"pushd . && cd layers && ./build-all.sh && popd\"",
    "cdk": "cdk"
  },
  "devDependencies": {
    "@types/node": "8.10.45",
    "typescript": "^3.3.3333",
    "ts-node": "^8.1.0",
    "aws-cdk": "^2.43.1"
  },
  "dependencies": {
    "@aws-cdk/aws-lambda": "^0.35.0",
    "@aws-cdk/cdk": "^0.35.0",
    "source-map-support": "^0.5.9"
  }
}
